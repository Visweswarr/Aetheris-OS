[package]
name = "polymera-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
uart_16550 = "0.2"
spin = "0.9"
x86_64 = "0.14"
lazy_static = "1.4"
serde = { version = "1.0", features = ["derive"] }

[features]
uefi = []
deterministic = []
debug = []
dev-keyvault = []

